{
  gateway: {
    mode: "local",
    // 容器内必须监听非 loopback，才能通过 docker 端口映射被宿主访问
    bind: "lan",
    port: 18789,
    auth: {
      mode: "token",
      // token 通过环境变量 OPENCLAW_GATEWAY_TOKEN 提供
    },
    http: {
      endpoints: {
        // 显式启用 OpenResponses 端点，避免依赖默认值
        responses: { enabled: true },
        // V1 不需要 ChatCompletions
        chatCompletions: { enabled: false },
      },
    },
  },
}
